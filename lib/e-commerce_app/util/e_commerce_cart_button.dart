import 'package:flutter/cupertino.dart';

class ECCartButton extends StatelessWidget {
  const ECCartButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
