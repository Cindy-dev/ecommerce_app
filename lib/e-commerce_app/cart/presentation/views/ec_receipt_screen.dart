import 'package:flutter/material.dart';

class ECReceiptScreen extends StatelessWidget {
  const ECReceiptScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
